<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.YourAppName" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar: slightly darker or matching dark primary -->
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- False = draw light icons/text on status bar (because the background is dark) -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>